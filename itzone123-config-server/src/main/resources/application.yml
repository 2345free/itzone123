server:
  port: 8888

logging:
  path: logs/
  level:
    root: info

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/2345free/itzone123-config-repo.git
          search-paths: weather
      profile: dev

