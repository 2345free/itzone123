spring:
  application:
    name: itzone123-config-server

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"

---
spring:
  sleuth:
    trace-id128: true
    sampler:
      probability: 1.0